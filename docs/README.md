# Aeon docs

Mintlify documentation for the Aeon Python SDK.

## Preview locally

```bash
npm i -g mint
cd docs
mint dev
```

Then open the URL printed in the terminal (usually `http://localhost:3000`).
